{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力：核心调用 + 系统通知",
  "windows": ["main", "float"],
  "permissions": ["core:default", "notification:default"]
}
