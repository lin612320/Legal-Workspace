# 更新日志

本项目的所有重要变更都会记录在此文件，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [赛事版 0.7.1] - 2026-09-11（分支 `feature/赛事版`，相对 0.7.0）

### 修复

- **0.7.1 覆盖安装报 `Error opening file for writing: …\win-unpacked-0.7.0\chrome_100_percent.pak`**：打包时误沿用了上一版的悬浮球资源目录名（`win-unpacked-0.7.0`），而常驻托盘的 0.7.0 悬浮球进程正锁着该目录里的 `.pak` / `.dll` / `icudtl.dat`，NSIS 覆盖写入失败（同一坑 0.3.1、0.3.2 已踩过两次）。修复：`tauri.conf.json` 的悬浮球资源目录随版本递增为 **`win-unpacked-0.7.1`**；主程序 `ball.rs::sorted_subdirs` 本就**优先拉起版本号最大**的目录，因此升级安装写入全新目录、与运行中的旧悬浮球互不干扰，旧球退出后自行失效。docs/04 补记「每次发版必须递增该目录名」的硬规则。
- **取消勾选待办报 `NOT NULL constraint failed: todos.done`**：`todos` 表里 `done` / `desktop_popup` 是 `INTEGER NOT NULL`，而 `todos_create` / `todos_save` / `todos_update` 用 `bool::then_some(1)` 写值——`false` 会写成 **NULL**，于是「勾选完成」正常、「**取消勾选**」直接撞 NOT NULL 约束（新建时关掉「桌面弹窗」也会同样失败）。修复：待办的全部 SQL 收敛到 `db.rs`（`todos_list` / `todo_create` / `todo_save` / `todo_update` / `todo_delete` / `todo_mark_notified`），布尔统一经 `flag()` 显式写 0/1；`todo_save` 影响 0 行时返回「待办不存在（id=…），可能已被删除」而不是静默成功。
- **底层报错不再裸奔**：新增 `db::friendly_db_err`，把 SQLite 原始信息翻译成可读中文（`NOT NULL constraint failed: todos.done` → 「完成状态不能为空（数据列约束）…」、`database is locked` → 「数据库正被占用…」、`no such table` → 「数据表缺失，可到数据设置从备份还原」等），随前端已有的失败提示一起展示。
- 新增回归测试 `db::tests::todos_create_save_and_uncheck_roundtrip`（新建 → 勾选 → **取消勾选** → 改回弹窗 → 局部更新 → 删除全链路，含 false 必须落 0 的断言）与 `friendly_db_err_maps_raw_sqlite_messages`；`cargo test` **17/17** 通过。
- **待办的「设置不生效 / 刷新就没了」根因**：Tauri v2 默认把 Rust 侧 snake_case 参数名映射成 **camelCase** 再从 JS 读取，旧代码传的是 `due_at` / `remind_minutes` / `desktop_popup`，被当成「缺参」处理：
  - `todos_save` 缺少必填 `remindMinutes` / `desktopPopup` → 整条命令报错，而 `callRust` 把错误吞掉返回 `null`，于是**勾选「完成」根本没写进数据库**、刷新后复选框复原、首页「进行中」恒为原值（只有「删除」能生效）；
  - `todos_create` 的截止时间 / 提前提醒 / 桌面弹窗被静默丢弃（`Option` 字段变 `None`），所以**刷新后待办里设置好的时间与提醒信息消失**。
  - 修复：`useTodos` 全量改为 camelCase 传参（`dueAt` / `remindMinutes` / `desktopPopup`），并新增 `invokeStrict`（失败抛错，不再静默）；`Todo` 页与首页对失败给出可见提示，首页在窗口重新聚焦时对齐后端数据。
- **新增待办后列表不刷新**：`todos_create` 成功后前端只写了后端、没更新本地 state，必须手动切页/重启才看得到。修复：新增/勾选/删除一律先写库、再立即更新本地列表，列表与首页统计即时刷新。
- **同类参数名 bug（一并修复）**：`templates_create` 的 `file_type` → `fileType`（此前「新建模板」静默失败）；`document_save` / `task_artifact_add` 的 `file_path` → `filePath`（此前交付物落「最近文书」时文件路径丢失，首页「打开」按钮不出现）。

### 变更

- **翻译不再提供内置免费接口**：移除 Google gtx 免费通道（国内网络不可达、译文不可控），文本翻译、文档全文翻译、智能体 `translate_text` 工具全部统一走「数据设置 → AI 接口」（DeepSeek / OpenAI 兼容）；「数据设置」删除独立的「翻译配置」（接口 / 地址 / Key 三项），改为说明卡，翻译与文书智能体共用同一份 `ai.*` 配置。
- **文档全文翻译（多模态）**：翻译页新增「文本翻译 / 文档全文翻译」两种模式与「选择文档」入口：
  - docx / pptx / xlsx / txt / md / csv 由 Rust **本地提取文本**（原件不上传）；切换 UTF-8 与 GB18030 解码，兼容老 Windows 中文文本；
  - pdf / 图片走支持文件或视觉输入的模型**转录全文**后再翻译，转录文本明确标注「模型转录，非原文」，与材料导入同一口径；
  - 长文档按段落分块（默认 2400 字/块）顺序翻译、逐块流式回显、显示「已译 n/N 段」并支持「停止」，术语与人名跨块保持一致；
  - 译文支持**导出 Word（.docx）**（桌面，同时记入首页「最近处理的文书」，可打开 / 定位）或**下载 .md**（浏览器预览）。
- 新增 Rust 命令 `extract_material_b64`（`<input type="file">` 拿不到绝对路径，故按「文件名 + base64」把字节临时落盘 → 复用 `office::extract_office_text` → 立即删除临时文件）。
- **发行渠道（首次建立）**：安装包发布到 **GitHub Releases**——<https://github.com/lin612320/Legal-Workspace/releases>（资产 `Faron-0.7.1-x64-setup.exe`，135.1 MB；git 单文件上限 100 MiB，故走 Release 资产而非仓库文件）；**产品演示站**上线 Cloudflare Pages：<https://faron-demo.pages.dev>（源文件 `官网演示/index.html`，单文件零依赖、可离线双击打开；含「引用自检交互实验室」与可交互知识图谱）。公开发布前已只读核验预置库无个人数据（`settings` / `documents` / `todos` / `chat_messages` 为空）。
- 文档同步：`docs/项目计划书.md` 升级 **v3.1**（对齐 0.7.1 发布实况、补记 0.7.0/0.7.1 里程碑、新增发布渠道与两条风险对策），并新增可复用的 Markdown → Word 转换脚本 `scripts/md2docx.py` 用于重新生成 `docs/项目计划书.docx`（此前 `.md`/`.docx` 成对却无同步工具）。
- 版本升至 **0.7.1**（`package.json` / `tauri.conf.json` / `Cargo.toml`）；工程名 / exe / 进程 / 数据目录不变，升级不丢数据。

## [赛事版 0.7.0] - 2026-09-09（分支 `feature/赛事版`，相对赛事版 0.6.0）

### 新增

- **文书智能体任务扩展为 5 类**：合同审查（`review`）/ 起诉状（`lawsuit`）/ 证据目录（`evidence`）/ 质证意见（`cross_exam`）/ 代理词（`argument`）：
  - `evidence`（证据目录）与后续表格型文书支持 **Markdown 表格 → docx 原生表格**（`docx.rs` 新增 `<w:tbl>` 渲染：内联边框、表头加粗灰底，不依赖 styles.xml）；
  - 新增内置模板「证据目录」「代理词（民事）」（老库启动时按标题幂等补齐，`db.rs::ensure_builtin_templates`）；
  - 各任务起草员步骤注入 `KIND_GUIDES` 输出骨架约束；任务类型白名单收敛为 `isTaskKind()`（原 `Agent.tsx` 两处硬编码同步）。
- **知识图谱（本版核心）**：
  - 新表 `kg_node` / `kg_edge`（SQLite，无图数据库、不联网）；5 类固定关系：构成要件是 / 请求权基础是 / 证明对象是 / 常见争点是 / 文书载体是；
  - **涉外案由优先的人工策展种子**（涉外货物买卖合同纠纷 / 涉外合资经营合同纠纷）；法条节点仅存"法律名"（民法 / 商法 / 会社法 / 民事訴訟法 / 美国法典 Title 15 等，已与预置库 152,940 条逐名核对存在），**不写具体条号、不编造**；
  - **规划与编排接入**：`planTask` 先按材料关键词匹配案由 → 图谱骨架驱动计划卡（`planFromSkeleton`：要件梳理 → 检索 → 模板（诉讼类）→ 起草 → 质检），模型模式把骨架注入规划提示作为约束，演示模式直接用骨架；要件不漏、法域自动限定；
  - **二段引用自检**：交付物「四之二、法条适用性校验（知识图谱）」与 QA 步骤【适用性校验 · 知识图谱】——引用法名是否落在案由请求权基础范围内（在确定性引用自检之后叠加）；
  - **候选边 + 人工确认**：任务完成把本次真实命中的法条登记为低置信度候选边（`kg_add_candidate`，已存在不重复），可视化中以虚线展示，点 ✓ 确认 / ✕ 拒绝后才作为规划依据；
  - 可视化面板（Agent 页）：分层 SVG 布局（案由 → 要件 → 争点 → 法条/证据 → 文书）、hover 邻接高亮、候选边虚线 + 行内确认、图例、命中案由高亮、空态；样式走 CSS 变量设计令牌，深浅主题可读。
- **多模态任务材料**（Agent 页「任务材料」面板，路径输入 + Enter 导入）：
  - 新模块 `office.rs`：**手写最小 ZIP 读取**（仅依赖 flate2，中央目录尺寸为准、兼容 store/deflate、拒 ZIP64 与非支持方法）+ DOCX（段落/单元格/制表符/软换行/实体反转义）、PPTX（按页码数字升序取 `<a:t>`）、XLSX（calamine 逐工作表）文本提取；单文件上限 20 万字符、UTF-8 边界截断；
  - **PDF 与图片**：走支持文件/视觉输入的模型（OpenAI 兼容 `content` parts：`image_url` / `file`；`read_file_base64` 上限 20 MB），转录文本带「模型解析，非原文，仅供整理参考」免责前缀，不进入原文引用口径；未配置模型时给出明确提示；
  - 材料文本并入任务材料文本框、随任务落库可续跑；新表 `task_attachments` + 历史任务载入时按需补回材料文本。
- 新增 Rust 命令：`kg_list / kg_query / kg_find_case / kg_confirm_edge / kg_add_candidate`、`attachment_add / attachments_list / attachments_text_all / attachment_delete`、`extract_material`、`read_file_base64`；依赖新增 `flate2` / `base64`（离线 crates 缓存命中）。

### 修复

- **人工确认闸门死锁（"起诉状等任务无法继续"根因）**：`need_confirm` 步骤点「继续（人工确认通过）」后再次命中闸门、无限回到 `waiting`，任务无法执行——`executeStep` 原先只看 `need_confirm`，`resumeFromGate` 未标记放行。修复：步骤新增运行期 `confirmed` 标记，放行一次后不再拦截；UI 显示「✓ 已人工确认」。
- **`need_confirm` 落库丢布尔**：`task_steps_save` 用 `as_i64()` 读布尔恒为 0，导致"需人工确认"重启后消失——改为兼容布尔 / 数字，重启后闸门保留且可正常放行一次。

### 变更 / 说明

- 版本升至 **0.7.0**（`package.json` / `tauri.conf.json` / `Cargo.toml`）；工程名 / exe / 进程 / 数据目录不变，升级不丢数据。
- **已出包（2026-09-09）**：`发布包/法元_0.7.0_赛事版_x64-setup.exe`（约 135.1 MB，内嵌法库预置库 + 悬浮球 1.0.1 装入 `win-unpacked-0.7.0` 版本化目录，升级不与运行中的旧悬浮球抢文件）与 `发布包/悬浮球助手 1.0.1_赛事版.exe`（便携版）。
- **多模态口径更新**：`docs/项目计划书.md` §5.5 与风险表由"多模态非本阶段目标"改为"已实现（PDF/DOCX/PPTX/XLSX/文本/图片材料输入；音频不在范围）"。
- 知识图谱定位：本地可溯源结构化知识（人工策展种子 + 任务命中候选边 + 人工确认），非模型自述；`source` / `confidence` 随节点与边落库。
- 已知边界：`docx.rs` 表格为首版（内联边框），不支持合并单元格 / 页眉页脚；浏览器预览模式不提供图谱与材料提取（同法库检索策略，需桌面版）；`vite build` 未在本环境出包（见交付说明），代码通过 `tsc` 与 `cargo test`（11/11）验证。

## [赛事版 0.6.0] - 2026-09-07（分支 `feature/赛事版`，相对 0.6.0「法元 · Faron」）

### 新增

- **法规查询升级为双模式**：
  - 「📚 按法浏览」：选择国家 → **国家专属页**（政体 / 国体 / 法律体系概览 + 按**法律领域**分组的法律列表，法律名展示**原文法名 + 中文译名**）；
  - 新增**整部法浏览页**（路由 `/law/:title`）：元信息（双语名称 / 领域 / 简介 / 条文数）+ 完整条文；支持**阿拉伯 / 中文数字 / 罗马数字条文跳转**、**打印 / 另存 PDF**；
  - 「🔍 关键词检索」支持**中文输入**：可命中 `title_zh` / `content_zh` / 整部法元数据译名（FTS 无命中自动回退 LIKE），检索结果一键打开整部法律。
- **数据库双语字段与元数据**：`laws` 增可空 `title_zh` / `content_zh` 列；新增 `laws_meta`（每部法：中文译名 / 原文名 / 领域 / 简介）、`country_intro`（国家概览）表；首启播种重点法元数据（日本国宪法、日本民法/刑法/公司法/劳动基准法/著作权法/民事诉讼法/刑事诉讼法、美国宪法、美国法典 Title 42/15/10/26/7/16、中华人民共和国民法典）。
- **法规 Excel 导入**支持可选 `title_zh` / `content_zh`（中文标题 / 译文）列。
- **美国宪法官方中译条文级内置**：启动迁移把「Preamble + Article I–VII」（25 段，与官方中译 PDF 对齐）按 `article_no` 幂等回填到 `laws.content_zh`（数据：`src-tauri/src/data/us_const_zh.json`，仅补空值、不覆盖导入译文）。
- **正文修复为可选中复制**（全局 `user-select` 放开，仅交互壳保留禁用）。

### 变更 / 移除（赛事演示口径）

- 侧栏由 10 版块精简为 **8 版块**：移除「AI 问答」（`/assistant`）与「专注计时」（`/focus`）及对应页面 / 路由；保留首页 / 法规查询 / 模板库 / 文书智能体（小元）/ 翻译 / 待办 / 数据设置 / 数据导入。聊天会话表与 AI 接口配置保留（供智能体与翻译使用）。
- **悬浮球（floating-ball）体验修复**：拖拽改为“起始位置 + 光标增量 + 主屏工作区钳制”，并在失焦 / 中断时兜底清理 `dragging` / `drop-hover` 卡死态（此前残留会造成球体放大 / 图标残留观感）；面板结果区新增 🗑 一键清空，新抓取 / 推送文本自动清空上一轮结果，⏹ / ⧉ / 🗑 仅在使用时显示。
- 工程标识（`legal-workbench` / `com.legalworkbench.app`）、数据目录与升级路径不变；主版本（dev）功能不受影响。
- **已出包（2026-09-07，赛事版）**：`发布包/法元_0.6.0_赛事版_x64-setup.exe`（约 135.1 MB，内嵌法库预置库 + 悬浮球 1.0.1 `win-unpacked-0.6.0`）与 `发布包/悬浮球助手 1.0.1_赛事版.exe`（便携版）。

### 修复（悬浮球收尾，2026-09-07）

- **提问面板 / 悬浮球“越拖越大”根治**：面板与小球的窗口尺寸改为固定值（`min = max`、`resizable:false`），并启动 **800ms 尺寸看门狗** 强制复位；拖动改为增量位移并钳制在主屏工作区内，任何系统/未知途径把窗口改大都会自动拉回。
- **抓取模式开关恢复可用**：此前 `pendingGrabMode` 未在模块级声明——未打开“设置”抽屉时直接点主面板的「自动抓取 / 手动拖入」会抛 `ReferenceError` 而无反应；已在模块级声明，并在启动时按配置同步高亮（无头 Electron 点击测试通过）。
- 悬浮球版本升至 **1.0.1**：托盘悬停提示可见版本号，避免单实例机制下“新包被旧常驻进程顶掉”造成假修复。

### 说明

- 译文口径：**美国宪法官方中译（正文 Preamble + Article I–VII）已条文级内置**；其 27 条修正案与其余法律（日法、美国法典各 Title）暂无现成译文，需要时以可溯源来源经 Excel（`content_zh` 列）或预置数据补齐，应用不生成无依据译文。

## [0.6.0] - 2026-09-06

### 新增 / 变更

- **品牌正式切换为「法元 · Faron」**（中文名「法元」，英文名 Faron）：`productName`、窗口标题、`index.html` 标题、侧栏品牌（律 → 法元）、系统通知标题、AI 助手系统提示词、交付物落款、悬浮球面板「推送到法元 / 关联查找提示」等用户可见文案由「律衡」切换为「法元 · Faron」。
- **文书智能体人设更名「小元」**（原「小衡」，随 0.4.1 定名、0.6.0 更名）：智能体页标题与引导语、执行日志、交付物落款（`由「法元 · 小元（…）」生成`）、演示话术统一为"把合同/案情交给小元"。
- **保持不变（升级安全）**：工程名 / 仓库 / 主程序 exe / 进程标识 / `identifier`（`legal-workbench`、`com.legalworkbench.app`）与用户数据目录不变，升级安装不丢数据。
- 版本升至 0.6.0（`tauri.conf.json` / `Cargo.toml` / `package.json`）。图标暂沿用 0.5.0「律」字标，0.6.0 出包前重设计。
- 文档口径：README、CHANGELOG、docs/01–09、项目计划书、功能演示报告同步至 0.6.0「法元 · Faron」；**中文全量法库路线图顺延至 0.7.0**（接入方案见 `docs/07` §八）。
- **已出包（2026-09-06）**：桌面 `发布包/法元_0.6.0_x64-setup.exe`（约 135 MB，内嵌法库预置库 + 悬浮球 `win-unpacked-0.6.0`；主程序 exe 仍为 `legal-workbench.exe`）；悬浮球重建为法元文案并同步便携版 `悬浮球助手 1.0.0.exe`；移动端 `律政工作台-移动端/发布/法元-移动端-演示-v0.1.0-debug.apk`（应用名「法元（移动演示）」）。0.4.x / 0.5.0（律衡）历史安装包与旧移动端包同目录保留。

## [0.5.0] - 2026-09-06

### 新增

- **悬浮球「关联查找法规」真正可用（走主程序本地法库）**：悬浮球面板「关联查找」不再提示"功能预留中"——选中/输入文本后经桥接文件请求主程序，主程序在本地 15 万条法库中检索并回传命中（篇名/条文号/来源/摘要），悬浮球面板直接展示；主程序未运行或浏览器预览模式时给出明确提示。实现：悬浮球 `main.js / panel-preload.js / panel.js`（新增 `laws:ask` / `laws:result` 通道）+ 主程序 `src-tauri/src/ball.rs`（`laws_search` action → `pick_keyword` 抽取关键词 → LIKE 检索前 8 条 → 回写 `from-workbench.json` 的 `laws_result`）。
- 移除 GitHub 上已废弃的 `feature/悬浮球外观` 分支（其 `lawsearch.js` 直读 SQLite 方案存在多进程占用风险，未吸收；改为上述主程序桥实现）。

### 变更

- **品牌正式硬更名**：`productName` 与安装包名由「律政工作台」改为「**律衡**」（安装包 `律衡_0.5.0_x64-setup.exe`），悬浮球资源目录随之改为 `win-unpacked-0.5.0`；`index.html` 标题、悬浮球面板「推送到律衡」等用户可见残留文案统一。**保持不变**：工程名/仓库 `legal-workbench`、主程序 exe 名、进程标识与数据目录（`com.legalworkbench.app`、`legal.db`）——升级安装不丢数据、悬浮球定位不受影响。0.4.x 及更早安装包沿用「律政工作台_0.4.x」属历史命名。
- 版本升至 0.5.0；README 与 `docs/01-08`、`docs/项目计划书.md` 的品牌/版本口径同步至 0.5.0（0.4.2 的确定性引用自检等历史记录保留）。

## [0.4.2] - 2026-09-06

### 新增

- **文书智能体·确定性引用自检（反幻觉硬校验）**：质检不再只依赖模型"自查"。新增规则级校验（`agent.ts` 的 `auditCitations`）：
  - 按中文/日文「第X条(之Y/のY)」、美国法典「§NN」等格式自动识别正文条文号引用；
  - 逐条判定引用是否能在「本次工具真实返回」的法条中找到对应条文，未覆盖项在交付物与质检日志中**点名提示人工核实**（不视为有效引用）；
  - 交付物新增「四、引用自检（确定性规则校验）」章节，QA 步骤末尾自动附带自检结论——评审可当场抽查"引用是否全部可反查"。

### 变更

- 版本升至 0.4.2，安装包 `律政工作台_0.4.2_x64-setup.exe`（内嵌法库预置库 + 悬浮球 `win-unpacked-0.4.2`，升级不覆盖旧版运行文件）。
- README / CHANGELOG 同步至 0.4.2；`docs/01-05` 由 0.1.0 口径同步至 0.4.x 现状，`docs/06-08` 参赛材料同步确定性引用自检新特性。

## [0.4.1] - 2026-09-06

### 变更

- **品牌采用传播名「律衡」（Legal Workbench）**：界面主标题、侧栏品牌、系统通知标题、AI 助手系统提示词、交付物落款等用户可见文案切换为"律衡"；工程名 / 仓库 / 进程标识 / 安装包名（`律政工作台_0.4.x_x64-setup.exe`）暂沿用"律政工作台 / legal-workbench"，正式硬更名随 0.5.0 执行（避免推翻已发布 0.4.x 安装包与文档）。
- **文书智能体人设名「小衡」**：智能体页标题与引导语、交付物落款、完成提示使用"小衡"，演示话术统一为"把合同/案情交给小衡"。
- 版本升至 0.4.1，安装包 `律政工作台_0.4.1_x64-setup.exe`（约 135 MB，内嵌法库预置库 + 悬浮球 `win-unpacked-0.4.1`）；README / CHANGELOG / 项目计划书 / 功能演示报告同步至 0.4.1。

## [0.4.0] - 2026-09-06

### 新增

- **文书智能体（Agent）模块**：把「对话助手」升级为可交付成果的「数字员工」。入口为左侧导航第 5 项「文书智能体」，内置三条流水线：**合同审查 → 审查意见书 / 起诉状起草 / 质证意见**（详见 `docs/06-参赛改进方案.md`）。
  - **与「AI 问答」分工 + 交接桥**：「AI 助手」更名为「AI 问答」（即时法律咨询），新增模块定位为端到端文书交付；AI 问答回答后点「📄 生成文书任务」可将最近一轮问答一键转交文书智能体（自动预填任务材料并匹配任务类型），实现"咨询 → 交付"闭环。
  - **任务理解与规划**：模型结构化输出计划卡（可编辑步骤 JSON、检索关键词与国家）；未配置模型时自动进入**演示模式**（使用预设流水线，工具步骤仍真实执行），保证浏览器预览也能演示全流程。
  - **自主工具调用循环（function calling）**：检索类步骤由模型在多个工具间自主决策连续调用，直至给出结论；工具包括 `laws_search / law_by_id / laws_country_preview / template_load / translate_text`，全程只引用工具真实返回的法条（可溯源、防编造）。
  - **人工确认闸门**：计划卡可编辑，步骤可标记「需人工确认」；执行支持暂停 / 继续 / 失败即停，并提供分步执行日志与累计引用法条展示。
  - **可验收交付**：交付物 Markdown 实时预览 → 桌面版一键导出 **.docx**（新增零依赖 OOXML + ZIP 生成器 `src-tauri/src/docx.rs`）并记入「最近文书」；浏览器版下载 .md。首页「最近处理的文书」面板点亮（`documents` 表补齐读写命令）。
  - **任务状态机落库（P1）**：新增 `tasks / task_steps / task_artifacts` 表与命令，任务与步骤状态持久化，支持中断后续跑与交付物归档。
  - **任务历史与续跑 UI（P1）**：智能体页新增「任务历史」列表（桌面从 SQLite 读取、浏览器保留最近任务快照），可一键载入历史任务查看进度 / 续跑 / 删除；步骤与任务状态实时回写数据库。
  - **多智能体接力可视化（P3 雏形）**：任务步骤按「规划 → 检索员 → 起草员 → 质检员」角色标注与图例展示，不同角色使用独立系统提示词与职责约束。
  - **交付后"打开 / 定位"闭环**：智能体导出 .docx 后可一键「打开文件」与「在文件夹中显示」；首页「最近文书」行内也提供「打开」（新增零依赖 `open_file / reveal_in_folder` 命令，白名单扩展名保护）。
  - **主应用 API Key 加密落盘**：`ai.api_key / translate.api_key` 不再明文存 SQLite / localStorage，改为 XOR+hex 混淆（前缀 `enc:`，与前端对称算法），启动时自动迁移历史明文；读取解密供 AI/翻译使用。
  - **真机验收清单文档**：新增 `docs/07-真机验收清单.md`（含评审演示关键词对照表与中文法库接入方案）。
  - **参赛材料包**：新增 `docs/08-参赛材料包.md`（创意描述、大模型环节流程、五维自评、竞品与商业模式、演示脚本、答辩 Q&A）。
- **法规检索 FTS5 全文索引（P2）**：为 `laws` 表建立 FTS5 外部内容表 + 同步触发器（`ensure_laws_fts`），≥3 字关键词按 BM25 相关度排序；运行时探测、任何失败自动回退原 `LIKE` 检索，预置库/旧库无需重建即可升级。
- **长期记忆（P2）**：任务完成后把摘要（+embedding，模型可用时计算）写入 `mem_vectors`；发起新任务时先做向量/关键词召回并注入规划上下文（桌面库内、浏览器 localStorage）。
- **悬浮球划词 → 智能体任务入口**：悬浮球推送的选中文字在智能体页自动预填为任务材料。
- 后端新增命令：`law_by_id / documents_list / document_save / document_delete / docx_export / task_create / task_list / task_get / task_set_status / task_delete / task_steps_save / task_step_update / task_artifact_add / mem_save / mem_list`。

### 修复

- **悬浮球自动拉起失败被静默吞掉（"点不开且无报错"反复出现的根因）**：主程序 `ball_start()` 的错误此前被 `let _ =` 丢弃。现改为：① `ball.rs` 启动时先带 `CREATE_BREAKAWAY_FROM_JOB` 拉起，若父进程位于不允许脱离的 Job 中导致 `ACCESS_DENIED`，自动去掉标志重试一次（代价为球与主程序同 Job，主程序退出时球随之退出）；② 启动失败写入控制台日志并向上抛给 `ball_show` 等命令；③ 前端点「🎯 悬浮球」失败时弹窗显示具体原因，不再无声失败。

### 变更

- 版本升至 0.4.0，安装包 `律政工作台_0.4.0_x64-setup.exe`（约 135 MB，内嵌法库预置库 + 悬浮球 `win-unpacked-0.4.0`）。
- 打包配置：`bundle.targets` 收敛为 `["nsis"]`（与历史交付一致，不再触发 MSI/WiX），`bundle.icon` 补入 `icons/icon.ico`，修复 `tauri build` 报 "Couldn't find a .ico icon" 无法出包的问题。
- 左侧导航由 9 版块增至 10 版块：新增「文书智能体」，并将「AI 助手」更名为「AI 问答」（咨询与任务交付命名区分）；相关 README 与文档索引同步。

## [0.3.2] - 2026-09-04

### 新增

- **内置日美法律数据库**：预置库 `src-tauri/preload/legal_preload.db` 随安装包分发（git 忽略，不入源码库），共 **152,940 条条文**（日本 2,104 部法 95,897 条 + 美国法典 53 Title 及宪法 57,037 条，另含 6 条内置示例）；首次启动检测到本地无 `legal.db` 时自动从安装资源拷贝装载——**装机即用，无需手动导入**。
- **悬浮球助手 1.0.0（随包更新）**：内置共享 API Key 改为混淆存储（XOR+base64，运行时解码，避免 `sk-` 明文裸露），设置保存时 API Key 落盘加密并兼容历史明文自动迁移；新增「抓取模式」设置（自动抓取 / 手动拖入——选中文字直接拖到悬浮球或面板即触发翻译检索）；AI 请求改走系统代理（Chromium `net.fetch`），办公网络/代理环境下可正常连接。
- **悬浮球可拉起打包版律政工作台**：此前仅能 spawn 开发服务器，现在自动定位并启动打包版 `legal-workbench.exe`（同目录 / 桌面），悬浮球也可作为入口独立拉起成品应用。
- **法规查询·按国家筛选与重点法规预览**：搜索栏顶部新增国家下拉（🌐 全部国家 / 🇯🇵 日本 / 🇺🇸 美国 / 🇨🇳 中国，带国旗图标与各库条数）；选中国家后预显示该国**条文最多的前 8 部重点法规**（点击即在所选国家内检索其法名），关键词检索也限定在所选国家范围内。国家归属在 SQL 层按标题/来源特征判定（日本 = e-Gov 来源、美国 = US Code/宪法标题或 govinfo/constitutioncenter 来源、中国 = “中华人民共和国”标题），无需迁移/重建预置库。

### 修复

- **全局钩子被永久禁用的历史问题**：`childMode` 曾会被误写入配置文件导致选词自动抓取静默失效，现改为仅运行时标志、绝不落盘。
- **抓取模式切换失效**：uiohook-napi 停止后重启偶发挂死，改为钩子常驻、仅切换 armed 开关。
- **升级安装报 “Error opening file for writing: \win-unpacked-0.3.1\icudtl.dat”**：0.3.2 初版打包误沿用 0.3.1 的悬浮球目录名，升级时会去覆盖仍在运行的旧悬浮球文件；已改为写入 **`win-unpacked-0.3.2`** 新目录，主程序在存在多个版本目录时**优先拉起版本号最大者**，升级安装不再与旧进程冲突。
- **升级后仍只有 6 条示例法规**：内置法库此前仅“本地无 legal.db”时装载；若用户曾启动过旧版生成了空库，升级后不会自动获得法库。现改为：本地库不存在 **或** laws 仍只有播种示例（≤6 条）时都自动装载预置库（旧空库先备份为 `legal.db.user-<时间戳>.db` 再覆盖），并对安装资源做浅层扫描、装载失败时输出明确日志。

### 变更

- 主应用版本升至 0.3.2，安装包 `律政工作台_0.3.2_x64-setup.exe`（约 141.6 MB，内嵌法库预置库 + 悬浮球）。
- 数据规模说明：库内法规正文约 350 MB，`LIKE` 关键词检索单次约数百毫秒级；「建立向量索引」不适用于对 15 万条全量 embedding，建议按需对检索结果切片调用 AI（详见 README）。
- **法规查询改为按需检索**：页面不再挂载即全量加载 15 万条条文（此前每次进入都通过 IPC 传数百 MB，切页必卡）；空关键词不请求后端，仅显示内置总数，输入关键词后才检索且命中最多返回前 500 条（可细化缩小）。原“向量检索”入口因不适用于该数据规模暂时下线（待接入 FTS5/分块向量后恢复）。

## [0.3.1] - 2026-09-03

### 修复

- **升级安装报 “Error opening file for writing”（悬浮球文件被占用）**：悬浮球常驻托盘导致旧版进程锁定自身文件（如 `win-unpacked\d3dcompiler_47.dll`），覆盖安装失败。新版把悬浮球装入**版本化子目录**（`win-unpacked-0.3.1`），升级时写入全新目录、不再覆盖被占用文件；主程序按有限递归自动定位任意版本子目录中的悬浮球。
- 若主程序本体正在运行，升级前请先退出律政工作台（悬浮球旧进程建议一并从托盘退出）。

### 变更

- 版本 0.3.1，安装包为 `律政工作台_0.3.1_x64-setup.exe`。

## [0.3.0] - 2026-09-03

### 新增

- **桌面悬浮球助手（floating-ball）**：桌面常驻悬浮球，选中文字即可一键翻译 / 关联查找 / 询问 AI；支持多套球皮肤与面板主题（皮肤选择窗）、悬停对话气泡。
- **悬浮球 ⇄ 律政工作台双向联动**：主应用启动自动拉起悬浮球；悬浮球可将选中文字推送到 AI 助手输入框；顶栏「悬浮球」按钮随时唤起面板。
- **命令通道重构为控制文件**：律政通过 `%APPDATA%\floating-ball\from-workbench.json` 下发 show / hide / prefill / translate / quit 命令，悬浮球常驻轮询执行——不再依赖二次进程传参（Electron 便携版 / 打包 exe 不透传命令行参数）。
- **发布形态一体化**：悬浮球（win-unpacked 自包含目录）随主应用安装包一并分发，安装后主程序自动定位并启动悬浮球。

### 修复

- **置顶悬浮窗空白并卡死**：AI 助手「置顶悬浮窗」此前在运行时动态创建窗口，Windows 下 `build()` 挂起导致页面空白、界面卡死；改为在 `tauri.conf.json` 预声明窗口，切换只做显示 / 隐藏，点「关闭」即收起并恢复主窗口。
- **悬浮球查找不到**：安装包资源落位修复（悬浮球随包装入 `win-unpacked/`）；主程序支持环境变量 `FLOATING_BALL_DIR`、exe 同目录 / `resources` / `_up_` 多级定位。
- **悬浮球打包态参数丢失**：命令行解析兼容打包产物（不再假定 argv 含应用目录占位）。
- **前端无法通过类型检查**：清除未使用导入与缺失类型声明，`tsc` 恢复全绿。

### 变更

- 主应用版本升至 0.3.0，安装包为 `律政工作台_0.3.0_x64-setup.exe`（内嵌悬浮球）。
- 独立悬浮球便携版仍可单独分发：`悬浮球助手 1.0.0.exe`（独立使用；与主应用联动请用随包版本）。
- README 同步更新：9 个版块（含专注计时）、悬浮球用法、打包与发布说明。

## [0.2.0] - 2026-08-30

### 新增

- AI 助手会话持久化到 SQLite：对话记录重启不丢，数字员工的"长期记忆"。
- AI 助手多会话管理：支持新建 / 切换 / 删除会话，首条消息自动生成会话标题。
- RAG 检索增强：提问时自动检索法规库相关法条并注入上下文，AI 回答可引用真实法条。
- AI 回复 Markdown 渲染：支持代码块、列表、加粗、行内代码、标题。
- AI 生成可随时中止：生成中显示「停止」按钮。
- 法规查询新增向量检索模式：基于语义相似度（embeddings + 余弦相似度）返回最相关法条，支持自然语言提问。
- 专注计时（番茄钟）版块：25/5/15 分钟模式、圆形进度、完成系统通知、番茄计数。
- 日间 / 夜间模式切换：顶栏一键切换，深色主题持久化。
- Excel 数据导入：支持将法规 / 模板数据从 Excel 批量导入本地库（中英文表头）。
- 待办桌面弹窗提醒：后台轮询到期待办并发系统通知，同一到期时间不重复提醒。
- 全新应用图标：渐变圆角 + 「律」字标识，替换原占位图标。

### 修复

- 顶栏「提醒」「设置」按钮接入导航（原为占位、点击无响应）。
- 关闭悬浮窗时自动恢复主窗口，避免应用"消失"。
- 悬浮窗与大窗口聊天记录实时共享。

### 变更

- 首个可分发版本：生成 NSIS 安装包（`律政工作台_0.2.0_x64-setup.exe`）。
- 补充项目文档：README 与 docs/01-05（技术架构、功能模块、数据库设计、开发构建指南、优化路线图）。
- 仓库瘦身：清理构建产物（target/dist）与工具链安装器（.install），源码仓库体积约 8.5 GB → 84 MB。
- 设置与翻译等本地数据存储方式不变，均为单机 SQLite / localStorage。

## [0.1.0] - 2026-08-28

### 新增

- 初始版本：Tauri 2 + React 18 + TypeScript 桌面应用骨架。
- 八大版块：首页总览、法规查询、模板库、AI 助手（含置顶悬浮窗）、翻译、待办提醒、数据设置、数据导入。
- 本地 SQLite 存储 + 浏览器预览降级（localStorage），支持双环境开发调试。
