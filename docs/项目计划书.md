# 法元（Faron · 工程名 legal-workbench）项目计划书（历史名：律政工作台 → 律衡）

> ⚠ 本文以**当前发布基线 0.7.1（赛事版，2026-09-11）**为准；0.6.0「法元 · Faron」为历史主版本基线，`feature/赛事版` 分支自 0.6.0 起的差异见 [11-赛事版改动说明.md](11-赛事版改动说明.md)。

> 版本：v3.1（2026-09-12：**对齐 0.7.1 发布实况**——0.7.0/0.7.1 连续发布（5 类文书任务 + 知识图谱 + 多模态材料 → 待办缺陷修复 + 翻译统一 AI 接口与文档全文翻译 + 覆盖安装缺陷修复）；里程碑表补记两版实际交付并区分"0.7.x 未完成项并入 0.8.0"；新增**发布渠道**（GitHub Releases 安装包直链、Cloudflare Pages 产品演示站）与两条风险对策（未签名安装包被 SmartScreen 拦、境外托管在国内的访问波动）；§4.1 / §5.1 / §七 / §九 同步。v3.0（2026-09-08：**把 0.8.0 主线「联邦配方进化」并入计划书**——0.7.0 中文法库接入之后的下一条产品主线；新增关联 [12-联邦配方进化方案.md](12-联邦配方进化方案.md)、§5.6 技术方案概要、§6.2 任务拆解与 0.8.0 里程碑行、§8 风险对策；联邦配方进化为**方案稿（未实现）**，如实标注"规划/🚧"，不影响 0.6.0「已落地」口径。v2.5（2026-09-07）内容全部保留：按"AI 杭州 码动未来"初赛五维评分标准增补——新增三章末《市场与商业可行性论证》、五章 5.4/5.5《大模型环节流程细节与架构取舍》，扩充八章风险对策；五维评分对照与自评属内部自查材料、不放入计划书正文，见 docs/08）｜日期：2026-09-12
> 关联文档：《README.md》《CHANGELOG.md》《docs/README.md》《docs/功能演示报告.md》《docs/06-参赛改进方案.md》《docs/07-真机验收清单.md》《docs/08-参赛材料包.md》《docs/09-真机验收执行指引.md》《docs/05-优化路线图.md》《docs/11-赛事版改动说明.md》《docs/12-联邦配方进化方案.md》（0.8.0 主线方案稿）《法库ETL工作区/ETL说明.md》《Project-Plan-EN.md / Project-Plan-EN.docx》《Feature-Demo-Report-EN.md / Feature-Demo-Report-EN.docx》（英文镜像，随本版同步）
> 代码仓库：主程序 https://github.com/lin612320/Legal-Workspace （dev 分支，tag `v0.6.0`；赛事版分支 `feature/赛事版`，最新 tag **`v0.7.1-赛事版`**，含 0.7.0 / 0.7.1 两次发布）｜移动端演示 https://github.com/lin612320/legal-workbench-mobile （main 分支）
> 发布渠道（0.7.1 起）：安装包 **GitHub Releases 直链**（<https://github.com/lin612320/Legal-Workspace/releases>，单文件 135 MB，公开仓库免登录下载）＋ **产品演示站**（Cloudflare Pages：<https://faron-demo.pages.dev>，单文件静态页，含引用自检交互实验室与知识图谱演示）
> 历史基线（0.6.0，供追溯）：桌面端 **v0.6.0「法元 · Faron」（已出包 + 已推送 GitHub）**（功能面同 0.5.0「律衡」，品牌与智能体人设已切换；`发布包/法元_0.6.0_x64-setup.exe`，2026-09-06；代码随 docs/09 一并提交并推送 dev、tag `v0.6.0`）已实现并内置日美法库与**文书智能体「小元」（数字员工，原「小衡」）**（含确定性引用自检）；主场景为**涉外/跨境法律业务**（审阅涉日美合同/文书，引用本地内置日美官方法条）；移动端演示版已重打为法元品牌 APK 并推送 GitHub（`发布/法元-移动端-演示-v0.1.0-debug.apk`，2026-09-06）。**赛事版（提交口径，2026-09-07）**：分支 `feature/赛事版`（tag `v0.6.0-赛事版`）——左侧导航收敛为 **8 版块**（移除「AI 问答」「专注计时」），法规升级为**国家 → 领域 → 整法浏览**并含双语（**美国宪法官方中译条文级内置**），悬浮球升级 **1.0.1**（窗口尺寸固定/看门狗、抓取模式开关修复）；赛事评审装机包为 `发布包/法元_0.6.0_赛事版_x64-setup.exe`（约 135 MB，含悬浮球 1.0.1）。**规划主线（0.8.0 起）**：中文全量法库接入顺延为 0.8.0 候选（docs/07 §八，任务拆解见 §6.1）；**0.8.0 联邦配方进化（方案稿，docs/12，任务拆解见 §6.2）**——本地只上传匿名"进化包"（统计/配方向量），云端加权聚合后签名下发更好配方，实现"智能体随用户需求与市场自我优化"。
>
> **最新发布（0.7.1 · 2026-09-11，分支 `feature/赛事版`，tag `v0.7.1-赛事版`）**：桌面端已出包 `发布包/法元_0.7.1_赛事版_x64-setup.exe`（约 135 MB）并发布 **GitHub Release**（含安装包直链与更新说明，免登录下载）；**产品演示站已上线**（Cloudflare Pages）。本版两条主线：① **待办缺陷修复**——Tauri v2 命令参数名（默认 camelCase）与布尔列 `NOT NULL` 落库缺陷，导致"勾选完成写不进库、**取消勾选报错**、设置好的截止时间与提醒刷新后消失"，已修复并补回归测试（`cargo test` **17/17**）；② **翻译能力重构**——移除内置免费接口，文本翻译 / 文档全文翻译 / 智能体 `translate_text` 统一走「数据设置 → AI 接口」，新增**文档全文翻译（多模态）**与**译文导出 Word / Markdown**。另修复"**覆盖安装报错**"（悬浮球资源目录随版本递增为 `win-unpacked-0.7.1`）。0.7.0（2026-09-09）交付内容：5 类文书任务、知识图谱规划编排与二段适用性自检、多模态任务材料、悬浮球 1.0.1。

---

## 一、项目概述

### 1.1 背景与定位

法律从业者（律师、法务、调解员等）日常工作高度依赖"查法规、写文书、问 AI、译外文"，而现有工具分散在浏览器 / 翻译器 / 通用 AI 之间，数据与上下文割裂；通用 AI 回答无法保证法条真实可溯源，云端产品又存在敏感数据出网风险。

**法元**（工程名：legal-workbench，历史名：律政工作台 → 律衡，英文 Faron）是一个本地优先的法律工作一体化应用：把「法规库 + 文书智能体（小元）+ AI 问答 + 翻译 + 悬浮球选词」收进一个单机应用。**数据全在本机**，敏感文书不出内网；内置日美法库（152,940 条条文，来源 e-Gov / govinfo 官方）开箱即用——**主场景定位于涉外/跨境法律业务**：审阅涉日本、美国法律关系的合同与文书（中日合资、赴美投资、出口管制合规等），「文书智能体「小元」」可自主查真实法条、起草并交付 .docx 文书——形成"听懂委托 → 制定计划 → 自主调用工具 → 交付可溯源成果"的端到端闭环（数字员工）。中文全量法库列入 0.7.0 路线图（按权威公开源接入，禁止编造条文，见 docs/07 §八）；当前中文能力以内置示例法条 + 中文文书模板演示。**0.8.0 规划（方案稿）**：借鉴联邦学习"数据不动、模型动"的范式，让「小元」在保持"数据不出本机"前提下实现自进化（联邦配方进化，docs/12）。

### 1.2 目标（历史 0.6.0 验收口径 → 当前发布 0.7.1）

- 功能面（0.5.0 已交付并出包）：法规检索（FTS5 优先）、AI 问答（RAG + 多会话 + 置顶悬浮窗）、文书智能体（规划 / function calling 工具循环 / .docx 交付 / 任务历史续跑 / 多智能体接力 / 人工闸门 / **确定性引用自检**）、API Key 加密落盘、交付文件打开 / 定位、悬浮球双向联动（含**关联查找法规**走主程序本地法库）。
- **0.6.0（品牌切换）**：`productName` / 窗口标题 / 侧栏 / 系统通知 / AI 提示词 / 悬浮球面板 / 交付物落款统一为「**法元 · Faron**」，文书智能体人设更名「**小元**」；工程名 / 主程序 exe / 进程 / `identifier` / 数据目录（`legal-workbench`、`com.legalworkbench.app`）不变；版本升至 0.6.0。
- 交付物（已出包，2026-09-06）：`发布包/法元_0.6.0_x64-setup.exe`（约 135 MB，内嵌法库 + 悬浮球 `win-unpacked-0.6.0`；移动端 `律政工作台-移动端/发布/法元-移动端-演示-v0.1.0-debug.apk`；0.4.x / 0.5.0「律衡」历史包同存于 `发布包/`）＋成套参赛文档（docs/06–09）。
- 代码发布（2026-09-06，已推送并打 tag）：主程序 `lin612320/Legal-Workspace` → `dev` 分支（含 0.6.0 品牌切换改动与 docs/01–09 同步，tag **`v0.6.0`**）；移动端演示 `lin612320/legal-workbench-mobile` → `main` 分支（更名「法元 · Faron」+ 翻译统一走 AI 接口）。
- 参赛口径：在"AI 杭州 码动未来"赛事中呈现"从问答助手到可交付成果的数字员工"的完整演进（材料见 `docs/08`），主演示场景为涉外/跨境日美法务。
- 路线图：**0.7.0** 中文全量法库接入（docs/07 §八，任务拆解见 §6.1）为主轴，配套数据安全（P0）、架构与体验（P1/P2）与工程化（P3）改进；**0.8.0（规划·方案稿）** 联邦配方进化（docs/12，任务拆解见 §6.2）——在保持"数据不出本机"前提下让智能体随用户需求与市场自我优化，作为中文法库接入后的下一条产品主线。
- **0.7.1（已交付，2026-09-11）**：待办数据落库缺陷修复（勾选完成 / 取消勾选 / 截止时间与提醒丢失）、翻译统一 AI 接口 + 文档全文翻译（多模态）+ 译文导出、覆盖安装缺陷修复；发行渠道补齐（GitHub Release 直链 + Cloudflare Pages 演示站）。**0.7.x 未完成项**（中文全量法库、备份定时调度与可恢复校验、SQLite 读写连接拆分、更新器 / CI 等，见 §6.1）**并入 0.8.0 候选池**，不计入 0.7.x 已交付口径。
- 参赛口径备注：五维评分标准的作品映射、自评与答辩预答属内部自查材料（见《docs/08-参赛材料包.md》《docs/09-真机验收执行指引.md》），不放入计划书正文。

---

## 二、范围与产品形态

| 形态 | 状态 | 范围 |
| --- | --- | --- |
| **桌面端（Windows，Tauri 2）** | ✅ **0.7.1 赛事版已出包并发布**（2026-09-11；分支 `feature/赛事版`，tag `v0.7.1-赛事版`；安装包经 **GitHub Release** 分发，见下） | 主版本 10 版块：首页 / 法规查询 / 模板库 / AI 问答 / 文书智能体 / 翻译 / 待办提醒 / 专注计时 / 数据设置 / 数据导入；内置法库 152,940 条；悬浮球随包分发（含「关联查找法规」主程序桥）。**赛事版提交口径收敛为 8 版块**（移除 AI 问答 / 专注计时，法规升级国家 → 领域 → 整法浏览，见 [11-赛事版改动说明.md](11-赛事版改动说明.md)）；**0.7.1**：翻译统一走 AI 接口并支持文档全文翻译与译文导出、待办落库缺陷修复 |
| **产品演示站（静态单页）** | ✅ 已上线（2026-09-11，Cloudflare Pages：<https://faron-demo.pages.dev>） | 单文件 HTML（零依赖、可离线双击打开）：功能实况（Tab 切换）/ **引用自检交互实验室**（可注入假条号看被点名）/ 知识图谱（hover 高亮 + 候选边人工确认）/ 数据事实带 / FAQ；安装包与文档入口指向 GitHub Release |
| **悬浮球助手（Electron 33）** | ✅ 随包分发 | 划词即用（翻译 / 关联查找法规 / AI）、多皮肤、双向文件桥；划词可预填文书智能体任务材料 |
| **移动端（Capacitor 演示版）** | 🚧 演示 APK（已推送 GitHub：`lin612320/legal-workbench-mobile`，main 分支） | 法规（国家 + 关键词）、AI 多会话、翻译（统一复用 AI 接口）、模板、待办、专注、主题；数据源已抽象 local / remote（服务端就绪后只读接入） |
| **后台服务端（规划）** | 🚧 规划 | 法库只读 API（`/api/laws/*`，协议见移动端 `src/laws/remote.ts`）／更新包／用量统计；一期不做账号与同步 |
| **联邦聚合服务端（0.8.0 规划·方案稿，docs/12）** | 🚧 方案稿 | 联邦配方进化聚合层：只接收白名单匿名"进化包"（统计 + 配方向量，原文零上云）→ 加权聚合 → 签名下发配方版本；最小可行形态 = HTTP + SQLite（无训练集群）；可部署为企业/律所内网私有联邦域 |

### 2.1 明确不做（边界；0.6.0 口径 + 0.7.1 补充）

- 云账号 / 多端同步 / 多人协作（本地优先是产品定位，不是缺陷）；
- **中文全量法库**（主场景已定位为涉外/跨境日美法务；中文法条按权威公开源接入，列为 0.7.0 路线图，见 docs/07 §八）；
- 移动端全量离线法库（490 MB 不适合手机默认分发）；
- 法规语义向量检索入口（15 万条不适合一次性全量 embedding，采用 FTS5 关键词召回 → Top 结果喂模型）；
- docx 复杂版式（表格化风险清单、批注修订版）——当前为最小 OOXML 子集；
- 自动备份定时调度（仍为配置 + 手动执行）；
- **联邦配方进化（0.8.0 规划，方案稿）**：默认关闭、opt-in；**不做**上传任何用户原文 / 文书 / 密钥，**不做**"联邦大模型权重"（模型走 API 无本地权重，联邦对象为编排配方，docs/12 §1.3 / §3.3）。
- **0.7.1 起移除**：内置免费翻译接口（Google gtx 通道，国内网络不可达且译文不可控）——翻译不再有独立通道，一律复用用户自配的 AI 接口；不提供"翻译历史 / 术语表 / 逐段对照"（列入 §6.1 候选）。
- **不做云端托管演示**：演示站为纯静态单页（功能展示 + 交互演示），不上传用户数据、不代管 API Key；真实功能体验仍以桌面安装包为准（数据不出本机是产品定位）。

---

## 三、用户场景与价值

1. **审涉日/美合同（端到端主场景）**：粘贴涉日合资、赴美投资、跨境贸易/出口合规类合同 → 文书智能体制定计划 → 自主检索本地内置的真实日美法条 → 起草《合同审查意见书》（中文输出，引用日美法条原文号 + 来源）→ 导出 .docx、引用可反查。
2. **写涉外文书**：起诉状 / 质证意见一键流水线，模板 + 法条双支撑，产出可二次编辑；检索词、法源、文书语言可按"涉日 / 涉美"分流。
3. **问 AI 要依据**：AI 问答注入真实法条，回答"引真条、不编造"；可一键"生成文书任务"转交智能体。
4. **查外法**：涉外业务查日本法 / 美国法典——"遺留分""trademark"国家内秒级命中，出处可标注（FTS5）。
5. **选词即用**：悬浮球在任意应用划词 → 翻译 / 关联查找（走主程序本地法库检索）/ 预填任务材料。
6. **中文法务延伸（路线图，0.7.0）**：接入中文全量法条后，同一套智能体能力服务于国内审合同 / 劳动纠纷等场景；当前中文以内置示例法条 + 中文文书模板演示。
7. **外出移动**：开庭 / 出差途中查法条、AI 摘要（移动端演示版）。
8. **智能体随使用自进化（0.8.0 规划·方案稿，docs/12）**：用户对计划卡的手工调整、检索命中与 0 命中、引用自检结果等本地日志，经匿名化聚合后回传"更好配方"——用户越多、任务越多样，「小元」默认流水线、检索词典与质检阈值越好用；同时聚合的需求缺口（0 命中词、新增任务类型分布）驱动产品路线排期（新法域、新文书模板、中文法库优先级）。

### 市场与商业可行性论证（参赛口径，对应评分项"市场可行性"）

**目标用户与使用价值（泛用性论证）。** 首批用户为三类高付费、强刚需人群：①涉外/跨境业务律师（中企出海、赴日美投资、跨境贸易/出口管制合规逐年增多，涉日美合同与文书审查是高频刚需）；②企业法务与合规岗（审阅涉外条款、翻译外国法原文、内部合规审查）；③法律研究与翻译岗。产品把"查法规、写文书、问 AI、译外文"四类散落工具收进一个单机应用，并给出通用 AI 与云端 SaaS 给不了的两点：**法条真实可溯源（不编造）+ 敏感文书不出网**。

**"窄市场"质疑的回应——法律域无关的可复制引擎。** 智能体管线（任务理解 → 规划 → 工具调用 → 质检 → .docx 交付）与法律域**解耦**：内置日美法库只是第一份"数据资产"，更换/扩充法库即可覆盖新场景。路线图已排定中文全量法库（0.7.0，docs/07 §八）——接入后同一套能力直接服务国内审合同、劳动仲裁、知产合规等更广阔市场；对律所/企业还可做**私有法库定制**（导入所内文书模板与常用法规）。因此"涉外日美"是**验证期聚焦场景**，不是能力上限。

**商业价值与盈利模式落地路径。** 三层结构：①免费基础层（法规浏览/检索、翻译、悬浮球划词）用于获客与建立信任；②任务订阅/按次层（数字员工执行合同审查、起诉状、质证意见——按次或订阅计费）；③企业私有化部署授权（律所/出海集团内网部署、法库更新服务、模板定制、操作审计——年费制）。定价锚点：一次审合同任务 ≈ 律师 1–2 工时，定价天花板取工时成本的小部分，地板取任务 token 成本；相比人工与云端 SaaS 均有明确价值空间。本地优先同时构成数据合规卖点（涉密文书不出内网），是律所/企业采购的关键决策因素。**0.8.0 规划的增值点**：企业私有化联邦域（所内聚合、越用越懂本所业务）与跨所匿名进化（docs/12 §6.3）可作为私有化授权档的续费理由与差异化能力。

**竞争优势与短板（诚实口径）。** 对比通用 AI 对话（无法条溯源、数据出网）与云端合同审查 SaaS（数据托管、无法条级溯源）：本作的差异点是"真实法条内置可溯源 + 引用经确定性规则自检 + 交付 .docx + 核心能力离线可用"。短板：无账号/协作/生态、无销售渠道与品牌、涉外场景市场教育成本高——属商业化阶段的工程量问题而非技术障碍，详见第八章风险对策。

---

## 四、功能范围

### 4.1 桌面端（主版本功能全集；0.5.0 已实现，0.6.0 品牌「法元 · Faron」；赛事版版块口径见 11）

首页总览（含最近文书）｜法规查询（国家筛选 + 重点预览 + FTS5 按需检索）｜模板库｜AI 问答（RAG / 多会话 / 置顶悬浮窗 / 一键转文书任务）｜**文书智能体「小元」（赛事版 0.7.0：合同审查 / 起诉状 / 证据目录 / 质证意见 / 代理词五类任务；计划卡 / 工具循环 / 人工闸门 / 历史续跑 / 接力可视化；知识图谱规划编排与法条适用性二段自检；多模态材料输入；交付 .docx（含表格））**｜**翻译（15 种目标语种 + 自动检测；0.7.1 起统一走「数据设置 → AI 接口」——长文按段落分块流式翻译；文档全文翻译：docx / pptx / xlsx / txt / md / csv 本地提取、pdf 与图片由支持视觉的模型转录（标注"非原文"）；译文导出 Word（.docx，记入最近文书）或下载 Markdown）**｜**待办 + 桌面提醒（0.7.1 起新增 / 勾选 / 删除即时同步列表与首页统计；写入失败给出可见原因）**｜专注计时｜数据设置（主题 / 备份 / 还原 / AI 接口，Key 加密落盘）｜数据导入（Excel）。

配套：

- 悬浮球助手（Electron）：抓取模式（自动 / 手动拖入）、皮肤主题、双向联动、Key 混淆加密、系统代理；悬浮球拉起失败报错可见 + Job 脱离降级。
- 内置日美法库：日本 2,104 部（95,897 条）+ 美国法典 53 Title 与宪法（57,037 条）+ 中国示例 6 条；条文级共 152,940 条；首启自动装载（空库 / 仅示例自动补装，旧库先备份）。
- 检索增强：`laws` 表 FTS5 外部内容索引（trigram + BM25），≥3 字关键词优先 FTS，短词 / 异常自动回退 `LIKE`。
- **发布渠道（0.7.1 起）**：安装包经 **GitHub Release** 分发（单文件 135 MB，公开仓库免登录直链；注意 git 单文件上限 100 MiB，故走 Release 资产而非仓库文件）；公开发布前需核验预置库无用户数据（`settings` / `documents` / `todos` / `chat_messages` 均为空）。

### 4.2 移动端（法元 · 轻量伴随入口，演示版已出）

- 法规：国家筛选 + 关键词 + 重点法规预览（数据源接口 local / remote 可切换，协议见 `src/laws/remote.ts`）；
- AI：多会话流式；翻译；模板 / 待办 / 专注 / 主题；
- 范围说明：数据存本机（localStorage）；演示口径为"法元的移动端生态演示"——不含桌面全量法库、Excel 导入、文书智能体任务与 .docx 交付（均为桌面能力）；在线检索源待服务端部署后启用（当前未启用）；法元品牌 APK 已重打（`发布/法元-移动端-演示-v0.1.0-debug.apk`，2026-09-06）。

### 4.3 数据资产

- 桌面预置库：`src-tauri/preload/legal_preload.db`（≈490 MB，git 忽略，随包分发）；
- 用户库：`%APPDATA%\com.legalworkbench.app\legal.db`；
- 加工链路：`法库ETL工作区/`（清洗 → 条文切分 → xlsx → 导入 / 预置），`ETL说明.md` 可复跑；
- 说明：法规原文版权归各官方源（e-Gov / govinfo / constitutioncenter / 国家法律法规数据库等），仅用于本地检索引用，分发注意合规边界。

---

## 五、技术方案

### 5.1 桌面端（现状）

Tauri 2（Rust）+ React 18 / TS + Vite 5 + SQLite（rusqlite bundled，WAL）；`laws_fts`（FTS5）；零依赖 OOXML docx 生成（`docx.rs`）；API Key XOR+hex 混淆（`keycrypt.rs`，启动迁移明文）；Electron 33 悬浮球；NSIS 打包（内嵌悬浮球与预置库）；双环境降级（桌面 SQLite / 浏览器 localStorage）。

**0.7.x 新增实现要点**：`office.rs` 办公文档本地文本提取（手写最小 ZIP 读取 + flate2，零外部转换服务）；`extract_material_b64` 命令（前端文件选择器只有字节、拿不到绝对路径，故按"文件名 + base64"临时落盘解析后立即删除）；翻译链路 `lib/translate.ts`（单通道 AI 接口 + 长文分段 + 流式）/ `lib/translateDoc.ts`（文档导入与译文导出）/ `lib/multimodal.ts`（PDF 与图片交模型转录，`lib/ai.ts` 提供多模态 content parts）；待办 SQL 收敛至 `db.rs`（`todo_create / todo_save / todo_update / todo_delete / todo_mark_notified`），布尔列统一经 `flag()` 显式写 0/1，底层报错经 `friendly_db_err` 转为可读中文；渲染层 `invokeStrict` 让写入类操作失败可见（不再被 `callRust` 静默吞掉）。

### 5.2 文书智能体技术要点

- 接入：OpenAI 兼容 `/chat/completions`，流式 + `tools`（function calling）+ 结构化 JSON（计划卡）；
- 引擎：`src/lib/agent.ts`——规划（模型 / 演示预设）、执行（模型驱动工具循环 ≤6 轮 / 确定性演示）、四角色提示词（规划 / 检索员 / 起草员 / 质检员）；
- 工具集：`laws_search / law_by_id / laws_country_preview / template_load / translate_text`（真实本地法库，可溯源）；
- 状态与记忆：`tasks / task_steps / task_artifacts / mem_vectors` 表；任务历史可载入续跑；跨任务向量 / 关键词召回。

### 5.3 检索演进（已落地）

`LIKE 全表扫描 → FTS5(trigram) + BM25（0.4.0 已实现，短词回退 LIKE）`；语义级"第二通道"列入远期（15 万条全量 embedding 不现实，建议关键词召回 + Top 结果喂模型）。

### 5.4 大模型环节流程细节（参赛创意描述口径：五环 + 旁路）

> 初赛要求创意描述含"使用大模型的环节流程细节"，以下为可直接引用/制作图解的文字版本。

**主链路（五环，数字员工一次端到端任务）**

1. **任务理解与规划**：LLM 结构化输出**计划卡**（步骤 JSON：3–8 步，含检索关键词与国家），前端可编辑、步骤可标记"需人工确认"；模型解析/规划失败时自动回退**预设流水线**并在界面明示"演示模式"（不静默降级）。
2. **工具决策与调用循环（function calling）**：模型在 `laws_search / law_by_id / laws_country_preview / template_load / translate_text` 间自主连续决策调用（≤6 轮），工具只返回**本地真实法条**并携带 `refs` 溯源；支持截断/超时/失败即停，关键步骤设人工闸门。
3. **起草**：起草员角色提示词约束——**只允许引用工具真实返回的法条**，输出固定结构文书正文（合同审查意见书 / 民事起诉状 / 质证意见）。
4. **质检（确定性引用自检）**：除模型复查外，执行器以**代码级规则**（非模型自查）解析正文中的"第X条 / 第X条のY / §N"等引用，逐一与本次工具真实返回比对，未覆盖引用在交付物与日志中**点名提示人工核实**、不计为有效引用。
5. **交付**：固定五段 Markdown（任务输入 / 执行过程 / 引用的法规条文（可溯源）/ 引用自检（确定性规则校验）/ 结论）→ 桌面一键导出 **.docx**（零依赖 OOXML）并记入首页"最近文书"、支持打开/定位；浏览器版下载 .md。

**旁路（赛事版口径）**：即时咨询不再单设 AI 问答栏——悬浮球面板"询问 AI"（本地法库检索增强）承担一问一答；需要交付成果时把材料直接交给小元走 1–5 闭环。

**降级矩阵（现场演示兜底）**

| 条件 | 行为 |
| --- | --- |
| 未配置模型 Key / 断网 | 演示模式：预设流水线 + 工具**真实执行**，全流程可跑 |
| 网关不支持 tools / json_object | 探测后回退预设流水线并明示"演示模式" |
| 模型规划解析失败 | 自动采用演示预设计划卡 |
| 浏览器预览（无 Tauri） | 数据降级 localStorage，工具环节以示例数据演示 |

### 5.5 记忆、长文本与赛道鼓励技术的对照（架构取舍，诚实口径）

- **记忆设计**：任务态持久化 `tasks / task_steps / task_artifacts`（中断可续跑、交付物归档）；跨任务长期记忆 `mem_vectors`（向量由前端调用 `/embeddings` 计算后入库，召回 LIMIT 500）。**如实说明**：长合同当前按 4k/6k 截断处理，完整的长文本按章节分块 + 要点抽取已列入 0.7.0（P1）。
- **检索增强（RAG）**：152,940 条/数百 MB 法库不做一次性全量 embedding（成本与时效不现实），采用"FTS5 关键词召回 → Top 结果喂模型"的务实 RAG；向量"第二通道"与命中重排列入远期。这是**数据规模下的工程取舍**，不是能力缺失。
- **多智能体对照**：当前为**单模型 + 四角色接力**（规划 → 检索员 → 起草员 → 质检员，各自独立系统提示词、执行链可视化）。状态机 + 角色解耦的架构已为 0.7.0 平滑升级到**真正多模型/多网关编排**预留空间；参赛口径如实表述为"单智能体内的多角色协作"，不夸大为多智能体系统。
- **多模态**：**0.7.0 起智能体材料输入已支持**——本地提取（离线可用）DOCX / PPTX / XLSX / TXT / MD / CSV（`office.rs`，手写最小 ZIP 读取 + flate2）；PDF / 图片需配置支持文件/视觉输入的模型（OpenAI 兼容 `image_url` / `file` content parts），转录文本一律带"模型解析、非原文"免责前缀，不进入原文引用口径。**音频不在范围**。法律文书交付仍以文本与 .docx 为主；悬浮球提供跨应用文本入口作为补充形态。

### 5.6 联邦配方进化技术方案概要（0.8.0 规划 · 方案稿，详见 [12-联邦配方进化方案.md](12-联邦配方进化方案.md)）

- **联邦对象（配方而非权重）**：计划卡模板 / 角色提示词参数 / 检索词典 / 质检阈值 / 记忆原型等 KB 级可审计编排资产；不做"大模型权重联邦"（模型走 API 无本地权重，云端亦不开放）；
- **本地侧**：复用现有任务日志（`tasks / task_steps / task_artifacts / mem_vectors`，无需新增采集）→ 特征提取（计划编辑 diff / 检索命中 / 审计通过率）→ 差分隐私 + K 匿名 → 产出 KB 级匿名"进化包"（默认 opt-in，可一键关闭）；
- **信号优势**：确定性引用自检（`auditCitations`）与计划卡人工编辑 diff = **零标注客观奖励信号**（代码规则产出，非模型自述、非用户问卷）；
- **聚合侧**：最小可行 = HTTP + SQLite；加权聚合（权重 = 样本量 × 审计通过率 × 新鲜度），只存聚合统计与配方版本、不存原文；
- **回程**：签名配方版本 → 本地 Shadow A/B → 指标改善后采纳，可一键回滚；
- **市场自适应**：聚合的 0 命中检索词 / 任务缺口驱动 0.9.0+ 产品路线（新法域、新任务模板、中文法库优先级）；
- **工程前置**：上行通道开通前将 Key 落盘/传输从混淆级升级为密码学加密 + TLS（现 `secret.ts` / `keycrypt.rs` 保持离线用途）。

---

## 六、里程碑与现状

| 版本 | 日期 | 里程碑 | 状态 |
| --- | --- | --- | --- |
| 0.1.0 | 2026-08-28 | Tauri + React 骨架，8 版块 | ✅ |
| 0.2.0 | 2026-08-30 | AI 会话持久化 / 多会话 / RAG / 专注计时 / Excel 导入 / 首个安装包 | ✅ |
| 0.3.0–0.3.2 | 2026-09-03~04 | 悬浮球双向联动 / 版本化目录 / 内置日美法库 / 国家筛选 / 按需检索 | ✅ |
| **0.4.0** | **2026-09-06** | **文书智能体 P0–P3 + FTS5 + Key 加密 + 交付打开定位 + 悬浮球拉起加固；NSIS 出包** | ✅ 代码 / 出包；真机验收待执行 |
| **0.4.1** | **2026-09-06** | **品牌采用传播名「律衡」；文书智能体人设「小衡」（补丁包）** | ✅ |
| **0.4.2** | **2026-09-06** | **确定性引用自检（代码级规则逐条比对引用与工具真实返回，非模型自查）** | ✅ |
| **0.5.0** | **2026-09-06** | **安装包 / 产品名曾更名「律衡」；悬浮球「关联查找法规」走主程序本地法库；主场景定位于涉外/跨境日美法务** | ✅ 出包（律衡_0.5.0_x64-setup.exe）；真机验收按 docs/09 执行 |
| **0.6.0** | **2026-09-06** | **品牌切换「法元 · Faron」、智能体人设更名「小元」；用户可见文案与文档口径同步；工程标识（legal-workbench / com.legalworkbench.app）不变** | ✅ 已出包并推送 GitHub（`lin612320/Legal-Workspace` dev 分支，tag **`v0.6.0`**；移动端 `legal-workbench-mobile` main）：`发布包/法元_0.6.0_x64-setup.exe`、`发布/法元-移动端-演示-v0.1.0-debug.apk` |
| **0.6.0-赛事版（参赛提交版本）** | **2026-09-07** | **分支 `feature/赛事版`：导航 10 → 8 版块（移除「AI 问答」「专注计时」）；法规升级为国家/领域/整法浏览 + 双语（双语法名、条文跳转、打印 PDF、正文可复制、中文检索、美国宪法官方中译条文级内置）；悬浮球 1.0.1（窗口尺寸固定 + 看门狗、抓取模式开关修复）** | ✅ 已出包并推送 GitHub（tag **`v0.6.0-赛事版`**）：`发布包/法元_0.6.0_赛事版_x64-setup.exe`（约 135 MB，含悬浮球 1.0.1）＋便携 `发布包/悬浮球助手 1.0.1_赛事版.exe` |
| **0.7.0-赛事版** | **2026-09-09** | **文书智能体扩展为 5 类任务（含证据目录 Markdown → Word 原生表格）；知识图谱（`kg_node` / `kg_edge`、人工策展种子、候选边 + 人工确认、图谱骨架驱动计划卡、法条适用性二段自检）；多模态任务材料（`office.rs` 本地提取 + PDF / 图片模型转录，带"非原文"标注）；悬浮球 1.0.1 赛事版** | ✅ 已出包并推送 GitHub（tag **`v0.7.0-赛事版`**）：`发布包/法元_0.7.0_赛事版_x64-setup.exe`（约 135 MB） |
| **0.7.1-赛事版（当前发布）** | **2026-09-11** | **① 待办落库缺陷修复**（Tauri v2 参数名 camelCase 与布尔列 `NOT NULL`：勾选完成写不进库、**取消勾选报错**、截止时间与提醒刷新后丢失；补回归测试 `cargo test` 17/17）；**② 翻译重构**（移除免费接口统一走 AI 接口；文档全文翻译（多模态）；译文导出 Word / Markdown）；**③ 覆盖安装修复**（悬浮球资源目录随版本递增 `win-unpacked-0.7.1`）；**④ 发行渠道**：GitHub Release 安装包直链 + 产品演示站上线 | ✅ 已出包并发布：`发布包/法元_0.7.1_赛事版_x64-setup.exe`（约 135 MB）；<https://github.com/lin612320/Legal-Workspace/releases> ；演示站 <https://faron-demo.pages.dev> |
| 0.7.x 未完成项（并入 0.8.0 候选池，拆解见 §6.1） | — | 中文全量法库接入（docs/07 §八）；自动备份定时调度与可恢复校验（P0）；全局状态 / SQLite 读写连接拆分 / 迁移版本化 / 智能体长文本分块（P1）；docx 表格化版式与体验增强（P2）；测试 / lint / CI / 更新器（P3） | 🚧 顺延至 0.8.0 候选 |
| **0.8.0（规划 · 方案稿，拆解见 §6.2）** | — | **联邦配方进化（docs/12）：本地匿名进化包 → 云端加权聚合 → 签名配方下发 → 本地 A/B 采纳**，实现"智能体随用户需求与市场自我优化"；含企业/律所私有联邦域 | 🚧 方案稿（未实现） |

> 真机验收清单见 `docs/07-真机验收清单.md`，逐项执行指引与记录模板见 `docs/09-真机验收执行指引.md`，完成后回填实测数据作为参赛实证。

### 6.1 0.7.x 计划拆解与落实情况（候选任务清单）

> 来源：`docs/05-优化路线图.md`（P0–P3 真实缺口，每项对应源码位置）与 `docs/07-真机验收清单.md` §八（中文法库接入方案）。排期建议：**M1 中文法库接入闭环 → M2 数据安全与架构稳定（P0+P1） → M3 体验与工程化（P2+P3 择要）**，每期验收以 docs/07 清单 + docs/09 回填为准，`npm run build`（tsc）与 `cargo test` / `cargo check` 全绿后出包。
>
> **落实情况（2026-09-11 回填）**：0.7.0 / 0.7.1 实际交付的是"文书智能体能力扩展 + 知识图谱 + 多模态材料 + 翻译重构与缺陷修复"，**下表 P0–P3 大部分仍未做**，如实标注：✅ 已落地 —— P1 的「`todos_update` / `todos_save` 收敛与参数类型修正」（0.7.1，含回归测试）、P1 的「全局数据状态」（0.7.1 以待办变更广播 `workbench:todos-changed` + 窗口聚焦对齐实现**局部**方案，完整数据层通知仍未做）、P2 的「翻译长文分段」（0.7.1）、P2 的「docx 表格化版式」（0.7.0，Markdown 表格 → Word 原生表格）、P3 的「Rust 单元测试」（部分：`db.rs` / `docx.rs` / `office.rs` 共 17 项）。**其余（中文全量法库、自动备份调度与可恢复校验、SQLite 读写连接拆分、迁移版本化、智能体长文本分块、命中高亮 / 树形浏览 / 分页、AI 会话滑动窗口、翻译历史与术语表、待办日历与重复提醒、lint / CI / 更新器）顺延为 0.8.0 候选池**。

| 优先级 | 任务 | 验收要点 | 来源 |
| --- | --- | --- | --- |
| P0 · 数据安全 | 自动备份定时调度（仿待办 30s 通知轮询，按 `backup.interval_days` 到点执行 `backup_now`）；备份可校验可恢复（先 `wal_checkpoint(FULL)` 或 `VACUUM INTO`，备份后打开校验 / 行数核对；`restore` 前先备份现状） | 到点自动出备份文件；还原后可打开、行数一致 | docs/05 P0 |
| P1 · 架构与技术债 | 全局数据状态（Context/Zustand 或数据层变更通知，解决 Sidebar/Home/Todo/Agent 跨页面不同步）；SQLite 读写连接拆分或连接池（慢查询移出全局 `Mutex`）；settings 批量读写合并 IPC；`todos_update`/`todos_save` 收敛与参数类型修正；数据库迁移版本化（`PRAGMA user_version` / migrations 表）；智能体长文本按章节分块 + 要点抽取后再起草 | 切页/跨组件数据实时一致；无全库阻塞；迁移可表达改表 | docs/05 P1 |
| P2 · 中文法库 + 体验 | **中文全量法库接入（0.7.0 主轴）**：权威公开源（国家法律法规数据库 flk.npc.gov.cn 等）→ 清洗为 `title / chapter / article_no / content / source` 导入表 → 运行时导入或并入预置库，首批《民法典》等常用法，规模如实标注；引用自检对中文「第X条」可反查。其余：docx 表格化版式（风险清单/批注）、关键词召回 Top 结果模型重排（不做 15 万条全量 embedding）、法规命中高亮 / 按篇树形浏览 / 分页、AI 会话滑动窗口、翻译长文分段、待办日历 / 重复提醒 | 中文条文检索 + 智能体引用可溯源；交付物版式达标 | docs/05 P2、docs/07 §八 |
| P3 · 工程化 | Vitest + Rust 单元测试（`db.rs`/`keycrypt.rs`/`docx.rs`/agent 纯函数）+ lint + CI；`tauri-plugin-updater`（自动更新）与 `tauri-plugin-log`；按需开启 CSP；启动脚本参数化（不再硬编码作者机器路径） | 测试 / lint / CI 全绿；安全项收敛 | docs/05 P3 |

### 6.2 0.8.0 计划拆解 —— 联邦配方进化（方案稿，详见 docs/12）

> 性质：**0.8.0 规划主线**（中文法库接入完成后的下一主线），方案稿状态如实标注；参赛口径作为**加分素材 + 路线图**，不进入 0.6.0 赛事版"已实现"清单（docs/08 §十 待办口径不变）。排期建议：**M1 本地信号层（可先行、完全本地） → M2 联邦协议与仿真 → M3 闭环下发与 A/B → M4 企业联邦与治理**。

| 优先级 | 任务 | 验收要点 | 来源 |
| --- | --- | --- | --- |
| P0 · 本地信号层 | 进化包 schema 定稿（白名单字段）；特征提取纯函数 + 单测（计划编辑 diff / 检索命中 / 审计通过率）；差分隐私加噪 + K 匿名判定；进化开关（settings，默认关） | 导出包无原文、KB 级、抓包可验证；`npm run build` / `cargo check` 全绿 | docs/12 §五 / §八 M1 |
| P1 · 联邦协议与仿真 | 聚合端（HTTP + SQLite）+ 加权 FedAvg（权重 = 样本量 × 审计通过率 × 新鲜度）+ 签名 manifest；3–5 仿真用户单机跑通收敛 | 收敛曲线 + manifest 校验通过；篡改包被拒 | docs/12 §4.2 / §七 / §八 M2 |
| P2 · 闭环下发与 A/B | Shadow 试运行（未来 N=10 任务并行评估）→ 采纳/回滚；本机进化日志（版本 / 指标变化，可删）；设置页管理 | 采纳前后指标对比表（计划卡编辑率↓ / 审计通过率↑ / 0 命中↓） | docs/12 §4.3 / §5.4 / §八 M3 |
| P3 · 企业联邦与治理 | 律所/企业内网私有联邦域（鉴权模型）；任务模板市场 + 贡献激励（可选）；聚合结果去标识公示（可选） | 私有化部署跑通所内联邦 | docs/12 §6.3 / §八 M4 |

---

## 七、质量与验收

1. 功能验收：`docs/功能演示报告.md` 所列演示路径全部可跑通；
2. 性能验收：法规检索（关键词）目标 ≤100 ms（FTS5 后应显著优于旧 LIKE）；实测数字按 docs/09 记录回填；
3. 质量闸门（0.7.1 实际执行口径）：`tsc`（`npm run build`）与 `cargo test`（**17/17**）/ `cargo check` 全绿后打包；装机冒烟（首启带库 / **覆盖升级不丢数据** / 悬浮球拉起）通过才发版；提交前扫描密钥与构建产物（`发布包/`、`dist/`、`src-tauri/target/` 均在 `.gitignore` 内）；
4. 文档同步：README / CHANGELOG / docs 与版本同步（0.7.1 已对齐；0.8.0 联邦配方进化按 docs/12 §7.3 验收清单执行）；
5. **发布渠道验收（0.7.1 新增）**：GitHub Release 资产直链可下载（HTTP 200 + `Content-Length` 与实际包体一致）；演示站可访问且交互项自检通过（无横向溢出、无 JS 报错、引用自检演示可复现"注入假条号 → 判定未通过"）；**公开发布前必须核验预置库无个人数据**（`settings` / `documents` / `todos` / `chat_messages` 为空）。
5. 联邦协议验收（0.8.0，docs/12 §7.3）：抓包确认载荷无原文/密钥字段；差分隐私生效（成员推断实验不还原单用户桶）；<K 匿名桶丢弃有日志；签名校验拦截篡改包；关闭开关/断网后任务与指标不劣化（回归）。

---

## 八、风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| 真机未全量验收（FTS 耗时 / docx 打开 / 真模型工具调用） | 实证不足 | 按 docs/07 清单 + docs/09 执行指引逐项验收并回填；0.6.0「法元」已出包并推送 GitHub（dev / tag `v0.6.0`，2026-09-06），功能面沿用 0.5.0 静态验证 |
| 中文全量法库未内置（0.7.0 路线图），国内法场景不能直接引用中文法条 | 若主叙事仍走"国内审合同"则口径冲突 | 主场景已定位为**涉外/跨境日美法务**（引用本地内置日美官方法条）；中文能力以内置示例法条 + 中文文书模板演示；docs/07 §七 演示关键词对照确保现场命中 |
| AI 依赖外部接口（Key / 网络 / 费用） | 演示失败或成本 | 演示模式（无 Key 全流程可跑）+ 系统代理 + 用量提示；本地功能离线可用 |
| 模型网关兼容性（tools / json_object） | 链路不稳 | 探测 + 降级：不支持 tools 回退预设流水线并明示"演示模式" |
| API Key 混淆（XOR+hex）非密码学加密 | 口径风险 | 表述为"避免明文落盘 / 防误读"；悬浮球同思路；0.8.0 上行通道开通前升级为密码学加密 + TLS（docs/12 §6.2 硬门槛） |
| 悬浮球拉起偶发失败 | 体验 | 0.4.0 已加固（Job 脱离降级重试 + 错误可见） |
| **覆盖安装报错 `Error opening file for writing`**（0.7.1 实发） | 用户无法升级 | 根因：打包沿用了上一版悬浮球资源目录（`win-unpacked-0.7.0`），常驻托盘的旧悬浮球锁住该目录 `.pak` / `.dll` / `icudtl.dat`。对策：资源目录**随版本号递增**（0.7.1 → `win-unpacked-0.7.1`，主程序优先拉起版本号最大者）；docs/04 已写入"每次发版必须递增"硬规则，并写明被占用时的手工处置（托盘退出悬浮球 / 结束进程后重试） |
| **安装包未做代码签名，Windows SmartScreen 报"已损坏 / 未验证"** | 首次安装受阻，影响评委与客户体验 | 发布页与演示站均写明一次性解除步骤（右键 → 属性 → 解除锁定，或"更多信息 → 仍要运行"）；后续可评估购买代码签名证书（成本项，见 docs/12 §6.2 同类硬门槛） |
| **演示站与安装包托管在境外（Cloudflare Pages / GitHub）** | 国内访问速度波动或偶发不可达 | 三重兜底：① 演示站为**单文件 HTML**，可直接邮件 / 微信发送，双击离线打开；② 现场用局域网演示（本地 `http.server`）；③ 安装包保留本地 `发布包/` 副本，必要时改用国内网盘或（备案后）国内 OSS / CDN |
| **公开发布预置库可能夹带个人数据**（135 MB 安装包内嵌 467 MB 预置库） | 隐私与合规风险 | 发布前用只读方式核验：`settings` / `documents` / `todos` / `chat_messages` 行数均为 0，`chat_sessions` 仅空会话标题 → 已确认 0.7.1 包体干净；将该核验写入 §七 质量闸门（第 5 条）作为发版前置步骤 |
| 法规内容合规 / 时效 | 引用风险 | 只读引用并标注来源；声明"仅供参考、以官方文本为准" |
| 评委/市场对"场景偏窄（仅涉外日美）、泛用性不足"的认知风险 | 市场可行性失分 | 三章末泛用性论证（法律域无关引擎 + 中文法库 0.7.0 路线 + 私有法库定制）；演示口径"验证期聚焦场景 ≠ 能力上限"，配合中文线镜头（内置示例 `第五百零二条 / 隐私权` + 中文模板） |
| 对"非多智能体 / 无语义检索 / 无多模态"的赛道技术质疑 | 综合创新性、赛道维度失分 | 5.5 架构取舍说明（数据规模下的工程取舍、角色化接力为多模型编排预留）；**多模态已落地（0.7.0：PDF/DOCX/XLSX/PPTX/文本/图片材料输入，音频不在范围）**；话术"智能体价值在端到端可验收，不在模型数量" |
| 实证材料不足（真机实测数据、截图录屏位未回填） | 技术可行性、赛道维度失分 | 按 docs/09 回填 FTS 实测耗时 / docx 打开 / 真模型工具调用；补齐法规页与 .docx 交付截图位；答辩以 docs/07 §七 必命中词保证现场演示成功 |
| 联邦配方进化为**方案稿（未实现）**，评委误认为"已落地"或质疑与"数据不出本机"卖点矛盾 | 口径/信任风险 | 一切场合标注"0.8.0 规划 · 方案稿（docs/12）"；主叙事维持 0.6.0 已落地实证；隐私口径为"最小化 + 差分隐私 + K 匿名"三层机制并可抓包验证（docs/12 §6.4）；默认关闭、离线核心链路不受影响 |
| "模型走 API、联邦是伪命题"的技术质疑（若评委深问） | 综合创新性失分 | 口径前置：联邦对象是编排配方（KB 级、可审计、可签名、可回滚），不是大模型权重；确定性自检信号复用为客观奖励信号（docs/12 §〇 / §2.3 / §3.3） |

---

## 九、附录

- 《功能演示报告.md》：演示路径、话术与口径；
- 《CHANGELOG.md》：0.1.0 → **0.7.1** 演进记录（0.5.0 律衡、0.6.0 法元 · Faron 品牌切换、0.7.0 智能体 5 类任务 + 知识图谱 + 多模态、0.7.1 待办与翻译修复 + 发行渠道）；
- 《法库ETL工作区/ETL说明.md》：数据来源、清洗规则、导入 / 预置方法；
- **《官网演示/index.html》**：产品演示站源文件（单文件、零依赖、可离线双击打开；`deploy/` 为部署产物，含 `og-image.png` 分享图、`404.html`、`robots.txt`、`_headers`；`_shots/` 为整页长图与分节截图）；线上地址 <https://faron-demo.pages.dev>；
- docs/06–09：参赛改进方案、真机验收清单、参赛材料包、真机验收执行指引（回填实测即参赛实证）；
- docs/11：赛事版改动说明（分支 `feature/赛事版` 差异清单）；
- docs/12：《联邦配方进化方案》（0.8.0 规划 · 方案稿——动机、可行性、架构、隐私、演示、里程碑与风险）；
- 《Project-Plan-EN.md / Project-Plan-EN.docx》《Feature-Demo-Report-EN.md / Feature-Demo-Report-EN.docx》：按赛事版基线同步的英文项目计划书与功能演示报告（本文件的英文镜像；**0.7.1 内容尚未回灌英文版，属已知待办**）；
- GitHub 仓库：主程序 <https://github.com/lin612320/Legal-Workspace>（dev 分支 tag `v0.6.0`；赛事版分支 `feature/赛事版` 最新 tag **`v0.7.1-赛事版`**；**Releases 页提供安装包直链**）；移动端演示 <https://github.com/lin612320/legal-workbench-mobile>（main 分支）。
