# 11 · 赛事版改动说明（相对 0.6.0「法元 · Faron」主版本）

> 适用分支：`feature/赛事版` ｜ 基线：dev `0.6.0`（2026-09-06）｜ 更新：2026-09-07（赛事版安装包出包与悬浮球收尾修复）
> 主版本（dev）不受影响；本文记录赛事版相对主版本的全部差异，作为赛事演示口径的“唯一事实来源”。

## 一、导航与版块（10 → 8）

| 变更 | 说明 |
| --- | --- |
| 移除「AI 问答」`/assistant` | 页面与路由删除（`src/views/Assistant.tsx` 移除）；聊天会话表与 AI 接口配置保留，供文书智能体与翻译使用 |
| 移除「专注计时」`/focus` | 页面与路由删除（`src/views/Focus.tsx` 移除） |
| 保留 8 项 | 首页总览 / 法规查询 / 模板库 / 文书智能体（小元）/ 翻译 / 待办提醒 / 数据设置 / 数据导入 |

- 置顶悬浮窗（AI 问答浮窗）随之不再有入口；`tauri.conf.json` 预声明的 `float` 窗口与 `float_in/out` 命令保留但默认隐藏、无调用方（不影响运行，可后续清理）。
- 悬浮球「推送到法元」的落点由 AI 问答输入框改为「文书智能体」任务材料；“询问 AI / 翻译 / 关联查找”仍由悬浮球面板自身完成。

## 二、法规模块改造（本版重点）

### 2.1 用户路径

```
法规查询(/laws)
├── [📚 按法浏览] 选国家(🇯🇵日本/🇺🇸美国/🇨🇳中国)
│     └── 国家专属页：政体/国体/法律体系概览 + 按法律领域分组的法律列表（原文法名+中文译名）
│           └── 点击法律名 → /law/:title 整部法浏览页
│                 ├── 元信息：双语名称/领域/简介/条文数/来源
│                 ├── 完整条文：默认官方原文、可选中复制、章节标题
│                 ├── 条文跳转：阿拉伯数字 / 中文数字 / 罗马数字（Article·Sec / Amendment / §）
│                 └── 打印 / 另存 PDF（WebView 打印，零新增依赖）
└── [🔍 关键词检索] 中文输入可命中 title_zh / content_zh / 元数据译名（FTS 无命中回退 LIKE）
```

- 旧“关键词命中条文 → 逐条阅读”的搜索体验保留（在检索模式下），并为“点开整部目标法律”提供一键入口（修复合规检索“点不到整法”的问题）。
- 正文可复制：全局 `user-select` 放开为 `text`，仅侧栏/顶栏/按钮等交互壳禁用。

### 2.2 数据模型（迁移自动执行，老库/预置库兼容）

| 变更 | 说明 |
| --- | --- |
| `laws.title_zh` / `laws.content_zh`（TEXT，可空） | 条文级中文译名 / 译文；Excel 导入与整法页“查看译文”开关共用 |
| `laws_meta(title PK, country, domain, name_zh, name_orig, intro)` | 整部法元信息：中文译名 / 原文名 / 法律领域 / 简介 |
| `country_intro(country PK, intro)` | 国家页顶部概览（政体 / 国体 / 法律体系），日/美/中各一行 |
| `idx_laws_title ON laws(title)` | 整法查询 / 分组提速 |

- 首启播种（仅当表空）：日/美/中重点法元数据（日本国宪法、日本民法/刑法/公司法/劳动基准法/著作权法/民诉/刑诉、美国宪法、美国法典 Title 42/15/10/26/7/16、中华人民共和国民法典）与国家概览。
- 检索 SQL：国家判定 CASE 改为**表别名限定**（`country_sql(alias)`），避免 join 后 `title` 歧义；检索结果回带 `title_zh`。

### 2.3 后端新命令

| 命令 | 返回 |
| --- | --- |
| `laws_country_home(country)` | 国家概览 + 领域分组法律列表（含 `title_zh` / 条文数），`未分类` 置后 |
| `law_page(title)` | 整部法元信息 + 完整条文（含 `content_zh`，按 id 顺序，上限 8000 条） |

### 2.4 真实译文口径（不编造法条）

- 术语：中文译名/译文仅接受**可溯源来源**；内置元数据的 `name_zh` 为通行的中文法律名称，`intro` 为事实性简介（不含自拟条文译文）。
- **已内置（条文级）**：美利坚合众国宪法官方中译——由源数据官方中译 PDF（`法律数据库（美.日）/…/pdf原件/美利坚合众国宪法/美利坚合众国宪法_译文.pdf`，美国国务院官方译本，正文 Preamble + Article I–VII）经“段落结构对齐”脚本（按「第X条 / 第X款」标题切分并与预置库 `article_no` 逐条比对，25/25 全对齐后才生成）产出 `src-tauri/src/data/us_const_zh.json`；启动迁移（`enrich_us_constitution_zh`）按 `article_no` 幂等回填 `laws.content_zh`，仅补空值、不覆盖导入译文。
- **未内置（需导入）**：该官方译本不含 27 条修正案；日本法、美国法典各 Title 无现成译文。需要时以可溯源来源按 `title` / `article_no` 整理为带 `content_zh` 列的 Excel 导入（或并入预置库后重新打包），应用不生成无依据译文。

## 三、悬浮球（floating-ball）修复

1. **拖拽不再“越拖越大 / 漂移”**：主进程按“起始位置 + 光标增量”移动并**钳制在主屏工作区内**；渲染层在 mouseup / 失焦 / dragend 时兜底清理 `dragging`、`drop-hover` 卡死态（卡死态会表现为球体停留在放大 / 换图标观感）。
2. **结果区按钮不常驻**：⏹ / ⧉ / 🗑 仅在使用时显示，避免“输入区上方常驻图标”。
3. **问答后清空结果**：新任务开始即清空；新增 🗑 一键清空；抓取 / 推送新文本自动清上一轮结果。
4. **提问面板“多次拖拽慢慢变大”根治（2026-09-07）**：面板窗口与悬浮球窗口均固定尺寸（`min = max`、`resizable:false`），并启动 **800ms 尺寸看门狗**——任何系统 / 未知途径把窗口改大都自动拉回；面板拖动改为增量并钳制在主屏内。
5. **主面板直接提供「自动抓取 / 手动拖入」快捷切换**：与“设置”抽屉联动（点击即生效并持久化）；并修复此前 `pendingGrabMode` 未声明导致“未开过设置抽屉时点击无反应”的问题（无头 Electron 点击测试通过）。
6. **版本升至 1.0.1**：托盘悬停提示显示版本号，可在单实例机制下确认运行的是新包（避免旧常驻进程顶掉新包造成假修复）。

## 四、验证与闸门

| 检查 | 结果 |
| --- | --- |
| `cargo check`（src-tauri） | ✅ 通过 |
| `tsc --noEmit` / `npm run build` | ✅ 通过 |
| 检索/浏览 SQL（对预置库副本执行迁移后实测） | ✅ 国家首页分组、整法页（含 content_zh）、中文“宪法”检索日本命中 104 条 |
| 美国宪法中译回填（对预置库副本执行 UPDATE） | ✅ 25/25 条命中并回填 `content_zh` |
| 悬浮球 JS（`node --check`） | ✅ 通过 |
| 抓取模式快捷切换（无头 Electron 点击测试） | ✅ 通过：点击 → 按钮高亮 + `saveConfig({grabMode})` 写入，无报错 |
| 赛事版安装包 | ✅ 2026-09-07 出包：`发布包/法元_0.6.0_赛事版_x64-setup.exe`（135.1 MB，含悬浮球 1.0.1） |
| 桌面端整机实跑（法规浏览/PDF/译文开关） | ⏳ 待真机执行（见 docs/09 指引回填） |

## 五、文档口径

- README / CHANGELOG / docs 索引已按本分支口径更新；docs/01–10 等详细技术文档以 0.6.0 主版本为基线（未逐条删除被移除模块的描述），差异一律以本文与 README 为准。
- 主版本（dev）文档保持 0.6.0 口径不变。

## 六、0.7.0 赛事版增量（2026-09-09，在 0.6.0 赛事版之上）

> 适用分支：`feature/赛事版`（0.7.0）｜更新：2026-09-09。**已出包**：`发布包/法元_0.7.0_赛事版_x64-setup.exe`（约 135.1 MB，悬浮球 1.0.1 装入 `win-unpacked-0.7.0`）与 `发布包/悬浮球助手 1.0.1_赛事版.exe`；真机验收待按 docs/07 / docs/09 回填。本节是 0.7.0 相对 0.6.0 赛事版的全部差异。

### 6.1 文书智能体任务：3 类 → 5 类

| 变更 | 说明 |
| --- | --- |
| 保留 | 合同审查（`review`）/ 起诉状（`lawsuit`）/ 质证意见（`cross_exam`） |
| 新增 `evidence` 证据目录 | 交付 .docx **原生表格**（`docx.rs` 新增 Markdown 表格 → `<w:tbl>`；内联边框 + 表头加粗灰底） |
| 新增 `argument` 代理词 | 独立输出骨架（争议焦点 → 事实 → 法律 → 请求 → 结语） |
| 起诉状更名 | 任务标签由「起诉状起草」改为「起诉状」；两处硬编码 kind 白名单收敛为 `isTaskKind()` |
| 起草约束 | `KIND_GUIDES` 注入起草员系统提示（各任务交付结构不同） |
| 内置模板 | 新增「证据目录」「代理词（民事）」，老库按标题幂等补齐 |

### 6.2 知识图谱（0.7.0 核心）

- **存储**：新表 `kg_node` / `kg_edge`（SQLite），节点类型 案由/要件/法条/证据种类/文书结构/争议焦点；关系固定 5 类（构成要件是 / 请求权基础是 / 证明对象是 / 常见争点是 / 文书载体是）；每节点/边带 `source` 与 `confidence`，可溯源。
- **种子（涉外优先，人工策展）**：涉外货物买卖合同纠纷、涉外合资经营合同纠纷；法条节点只存法律名，已与内置预置库逐名核对（民法 1165 / 会社法 1077 / 民事訴訟法 474 / 美国法典 Title 15 2634 条等），不写条号、不编造。
- **规划与编排**：`planTask` 先按材料关键词命中案由 → `planFromSkeleton` 用图谱骨架生成计划卡（要件梳理 → 检索 → 模板 → 起草 → 质检），模型模式把骨架注入规划提示作约束，演示模式直接用骨架。
- **二段引用自检**：确定性引用自检之后叠加「法条适用性校验」——引用法名是否落在案由请求权基础范围（交付物「四之二」+ QA 步骤）。
- **自进化（人工确认制）**：任务完成后真实命中的法条自动登记为低置信度候选边（`kg_add_candidate`），可视化虚线展示，**人工 ✓ / ✕ 确认后才作为规划依据**（`kg_confirm_edge`）。
- **可视化**：Agent 页知识图谱面板——分层 SVG、hover 邻接高亮、候选边虚线 + 行内确认、命中案由高亮、图例、空态；样式走 CSS 变量令牌。

### 6.3 多模态任务材料

- **本地提取（离线可用）**：DOCX / PPTX / XLSX / TXT / MD / CSV。新模块 `src-tauri/src/office.rs`：手写最小 ZIP 读取（仅 flate2；中央目录尺寸为准，兼容 store/deflate）+ docx（段落/单元格/表格/制表符/换行/实体）、pptx（按页码数字升序）、xlsx（calamine）文本提取；单文件上限 20 万字符、UTF-8 边界截断。
- **PDF / 图片**：需配置支持文件/视觉输入的模型（OpenAI 兼容 content parts：`image_url` / `file`；`read_file_base64` 上限 20 MB）；转录文本带「模型解析，非原文」免责前缀，不进入原文引用口径。**音频不在范围**。
- **落库**：新表 `task_attachments`；材料文本并入任务材料并随任务续跑；历史任务载入时按需补回材料文本。

### 6.4 修复（0.7.0）

- **人工确认闸门死锁**：`need_confirm` 步骤点「继续」后无限回到 waiting（曾表现为"起诉状等任务无法执行"）。修复：步骤新增运行期 `confirmed`，放行一次不再拦截；UI 显示「✓ 已人工确认」。
- **`need_confirm` 落库丢布尔**：`task_steps_save` 兼容布尔 / 数字，重启后闸门保留且可正常放行。

### 6.5 命令与版本

- 新增命令：`kg_list / kg_query / kg_find_case / kg_confirm_edge / kg_add_candidate`、`attachment_add / attachments_list / attachments_text_all / attachment_delete`、`extract_material`、`read_file_base64`。
- 依赖新增：`flate2`、`base64`（离线缓存命中，无网络依赖）。
- 版本：0.7.0（package.json / tauri.conf.json / Cargo.toml）。
- 多模态口径：`docs/项目计划书.md` §5.5 与风险表由"非本阶段目标"改为"已实现"。
- 验证：`npm run build`（tsc + vite）✅；`cargo test` 13/13 通过（office 9 + docx 2 + kg/材料 2）；**已出包** `发布包/法元_0.7.0_赛事版_x64-setup.exe`（约 135.1 MB）。桌面真机验收（图谱可视化 / 5 类任务 / 多模态材料 / 证据目录 docx 表格）待按 docs/09 回填 docs/07。
